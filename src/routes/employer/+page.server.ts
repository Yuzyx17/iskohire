import { test_employ_form } from "./store";

export function load({ cookies }) {
	return {
		
	};
}

export const actions = {
	create: async ({ cookies, request }) => {
		const data = await request.formData();


	},
    update: async ({ cookies, request }) => {
		const data = await request.formData();


	},
	delete: async ({ cookies, request }) => {
		const data = await request.formData();

	}
};